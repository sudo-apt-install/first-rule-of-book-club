  <div class="container">
    <h1 class="mt-5 mb-4">Search Results</h1>
    <ul class="list-group">
      {{#each data}}
      <li class="list-group-item">{{this.volumeInfo.title}}</li>
      {{/each}}
    </ul>
    <p class="mt-3">Total Items: {{total}}</p>
  </div>